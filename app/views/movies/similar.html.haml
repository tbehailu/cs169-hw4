%h1 Similar Movies

%table#movies
  %thead
    %tr
      %th{:class => ("hilite" if @sort_var == "title")}= link_to "Movie Title", movies_path(:sort_var => :title), {:id => "title_header"}
      %th Rating
      %th{:class => ("hilite" if @sort_var == "release_date")}= link_to "Release Date", movies_path(:sort_var => :release_date), {:id => "release_date_header"}
      %th More Info
  %tbody
    - @movies.each do |movie|
      %tr
        %td= movie.title 
        %td= movie.rating
        %td= movie.release_date
        %td= movie.director
        %td= link_to "More about #{movie.title}", movie_path(movie)